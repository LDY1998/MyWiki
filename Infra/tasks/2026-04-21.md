---
title: 2026-04-21 Tasks
type: task-note
status: active
tags:
  - tasks
updated: 2026-04-21
---

# 2026-04-21 Tasks

## Todos

- [ ] Revisit [[Ultra-Scale Playbook]] notes and turn the loose 2026-04-20 takeaways into durable updates for [[Parallelism Strategies]], [[Tensor Parallelism]], [[Pipeline Parallelism]], [[FSDP]], and [[ZeRO]].
- [ ] Read [[Transformer Math 101]] with focus on parameter, gradient, optimizer-state, activation, and FLOP formulas.
- [ ] Do the Phase 0 memory math drills for 125M, 1.3B, and 7B models; capture durable formulas or worked examples in [[Transformer Memory Math]].
- [ ] Read or skim [[NCCL User Guide]] around collective communication patterns; verify the wiki clearly distinguishes `all_reduce`, `all_gather`, `reduce_scatter`, `broadcast`, `scatter`, `gather`, and point-to-point send/recv.
- [ ] Add missing durable coverage for context parallelism, sequence parallelism, and expert parallelism, even if Phase 0 later defers deep EP/CP work.
- [ ] Draft the first implementation outline for `mini-dist-trainer`, starting with `memcalc.py` requirements and the collective trace expected from FSDP/ZeRO-3.

## Read

## Done

## Follow-ups

- Phase 0 is still light on exact memory accounting. Strengthen the ability to derive bytes per parameter for BF16/FP16 training with FP32 master weights, Adam optimizer state, activation checkpointing, ZeRO/FSDP sharding, QLoRA, and KV cache.
- The collectives mental model needs more precision. Be able to predict before/after tensors for each collective and explain where each appears in DDP, FSDP/ZeRO-3, TP, PP, and Ray-style actor systems.
- Existing notes mention DP/TP/PP/FSDP/ZeRO, but CP, SP, and EP need clearer wiki coverage and explicit links to how they matter for world-model/RL infrastructure.
- Hands-on Phase 0 work has not started yet. Prioritize `collectives_demo.py`, then `memcalc.py`, before spending more time on optional readings.
- Ray actor basics are still missing from the current task trail. Schedule a follow-up day for `ray.put`, `ray.get`, `ray.wait`, object-store behavior, and the learner/rollout-worker skeleton.
- The graduation path requires an artifact, not just reading: `mini-dist-trainer` should eventually include `memcalc.py`, `train_gpt.py`, `ray_rollout/`, measured memory/throughput logs, and a README writeup.
